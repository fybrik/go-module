apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "go-module-server.fullname" . }}
{{- if .Values.image.pullSecret }}
imagePullSecrets: 
  - name: {{ .Values.image.pullSecret }}
{{- end }}